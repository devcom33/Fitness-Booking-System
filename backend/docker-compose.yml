services:
  db:
    image: postgres:15
    ports:
      - "5430:5432"
    restart: no
    env_file:
      - .env
    environment:
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${POSTGRES_DB}

  adminer:
    image: adminer:latest
    ports:
      - "8899:8080"
    restart: no